# =============================================================================
# Placeholder secrets file for the NixOS host `linux`.
#
# This file is intentionally UNENCRYPTED and empty. Do NOT put real secrets
# here until you have:
#   1. Provisioned YubiKeys per the runbook Phase 0.
#   2. Installed NixOS on the target and generated its OpenSSH host key.
#   3. Populated the placeholder recipients in ../../.sops.yaml with real
#      age1... values (including the target's ssh-to-age recipient).
#   4. Encrypted this file with `sops -e -i secrets/hosts/linux.yaml`.
#
# `nixos/system/sops.nix` currently declares no `sops.secrets.*` entries, so
# activation on the target will not attempt to decrypt this stub. When you
# start adding real secrets, uncomment the matching declarations in
# `nixos/system/sops.nix` and populate keys here via `sops secrets/hosts/linux.yaml`.
# =============================================================================
placeholder: null
